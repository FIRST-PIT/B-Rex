package dev.brex.analysis;

/** The physical unit of an analysis value, so output layers can format it consistently. */
public enum Unit {
    SECONDS,
    METERS,
    RADIANS,
    METERS_PER_SECOND,
    COUNT,
    PERCENT,
    NONE
}
